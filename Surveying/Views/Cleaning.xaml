<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Surveying.Views.Cleaning"
             Title="Cleaning">
    <VerticalStackLayout>
        <Label 
            Text="Cleaning"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

        <HorizontalStackLayout>
            <Label Text ="Tanggal Cuci :"/>
            <DatePicker x:Name="CleanStartDate"/>
        </HorizontalStackLayout>
        
        <HorizontalStackLayout>
            <Label Text ="Tanggal Selesai :"/>
            <DatePicker x:Name="CleanEndDate"/>
        </HorizontalStackLayout>

        <Button Text="Upload Photo" Clicked="OnUploadPhotoClicked" />

        <CollectionView x:Name="PhotosCollectionView" ItemsSource="{Binding Photos}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    
                    <StackLayout Spacing="5" Padding="5">
    
                        <Image Source="{Binding ImageSource}" HeightRequest="100" Aspect="AspectFill" />
                     
                        <Button Text="Delete" Clicked="OnDeleteClicked" />
                        
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </VerticalStackLayout>
</ContentPage>